# Brainfuck Interpreter

A simple [Brainfuck](https://en.wikipedia.org/wiki/Brainfuck) interpreter written in Zig.

## Building and Running

Example script to build and run.

```sh
$ zig build
$ cat ./res/hello.bf | xargs -0 zig build run --
```
